// Minimal sprite rendering mode
// Shader logic is in shaders.ts (MINIMAL_FRAG)
// This module exists for Phase 6+ when mode-specific settings are needed

export const MINIMAL_MODE_CONFIG = {
  baseSize: 3.0,
  softEdge: 0.3,
};
