// Controls module — placeholder sliders for Phase 0
// Full implementation in Phase 6

export interface ControlValues {
  temperature: number;
  speed: number;
}

export const DEFAULT_CONTROLS: ControlValues = {
  temperature: 2.0,
  speed: 1.0,
};
